var hora = 20; // horas
var minutos = 30; // minutos
var segundos =45; // segundos

var totalSegundos = (hora * 3600) + (minutos * 60) + segundos;

console.log("El total de segundos es: " + totalSegundos);